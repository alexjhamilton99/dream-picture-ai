package settings

import "dream-picture-ai/view/layout"
import "dream-picture-ai/types"

templ Index(user types.AuthenticatedUser) {
  @layout.App(true) {
    <div class="max-w-2xl w-full mx-auto mt-4">account</div>
  }
}
